
class Todos {
    constructor(id, userId, title, completed) {
        this.id = id,
            this.userId = userId,
            this.title = title,
            this.completed = completed
    }
}
module.exports = { Todos };